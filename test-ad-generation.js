const { generateAdCopy } = require('./.next/server/chunks/active_gh-creative-dashboard_lib_create_ad-generator_ts_db3df77d._.js'); 
const { createClient } = require('@supabase/supabase-js');
const dotenv = require('dotenv');
dotenv.config({ path: '.env.local' });

// We need a script to test the retriever.
